export interface IGame21Result {
    userScoreValue: number;
    opponentScoreValue: number;
    returnedCards: string;  // IReturnedCards
    cardsPlayed: string;  // ICardsPlayed
    gameState: string;  // enum
}